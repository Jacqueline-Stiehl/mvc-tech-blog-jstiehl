{{#each blogs as |blog| }}
<div class="row mb-4 blog">
  <div class="col-md-5">
    <h2>
      <a href="/blog/{{blog.id}}" style="color: rgb(19, 131, 141)">{{blog.title}}</a>
    </h2>
    <p class="posted-blogs">Posted on {{format_date blog.date_created}}</p>
  </div>
</div>
{{/each}}
